{
  "name": "Air, live reload for Go apps",
  "id": "air",
  "version": "1.0.0",
  "description": "Installs Air, a live reload tool for Go apps",
  "options": {
    "version": {
      "type": "string",
      "proposals": ["latest", "v1.52.3"],
      "default": "latest",
      "description": "Select version to install, or choose 'latest'"
    }
  }
}
